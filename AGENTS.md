# AI 协作入口

本仓库使用 `.agents/` 作为 AI 协作规则的事实来源。所有 AI 协作者在分析、回答、搜索文件、运行命令或修改代码前，都必须先阅读本入口和全局规则。

## 读取顺序

1. `AGENTS.md`
2. `.agents/global/README.md`
3. `.agents/global/architecture.md`
4. `.agents/global/code-style.md`

## 硬性门禁（必须遵守）

1. 按“读取顺序”逐份阅读，并以当前仓库规则为准。
2. 本项目是以 Android 智能柜触屏终端为主要目标的 `Flutter + Dart` 应用，同时包含 Kotlin/Gradle Android 平台代码、Camera2/MediaCodec/RKMPP/RTSP 推流能力和 C++ JNI 桥接；不要套用 Web 管理后台、Electron、普通手机 App 或纯原生 Android 项目的目录约定。
3. **禁止 AI 自行进行 Git 写操作**：
   - AI 不允许执行 `git add`（含 `git add -A/-u/-p/-f`）、`git commit`、`git commit --amend`、`git push`、`gh pr create` 等会改变暂存区、提交历史或远端状态的命令。
   - 如需暂存、提交、推送或创建 PR，必须由用户手动执行，或由用户在对话中明确授权。
4. **禁止 AI 使用 git worktree 修改**：
   - AI 不允许创建、切换或依赖 `git worktree` / linked worktree 来执行本仓库修改。
   - 所有代码、规则和文档修改都必须在当前源文件工作区内完成。
5. `android/app/src/main/jniLibs/` 中包含预编译原生库。未经用户明确要求，不删除、替换或重新生成其中的 `.so` 文件；修改 `rkmpp_bridge.cpp` 后也不能假定普通 Flutter 构建会自动更新预编译库。
6. 当前 Android `release` 构建仍使用 debug key，仅可用于设备验收，不能宣称为正式发布包。
7. 对文件、目录、生成物、本地产物或设备侧资源是否保留存在疑问时，必须由用户确认后再处理。

## 当前业务硬性门禁

1. **登录先于任务**：首页只展示柜况、统计和登录入口，不恢复匿名存取或飞检直达入口。正式模式下账号密码只用于确定账号，不计入身份因子；普通任务必须由同一账号完成人脸、指纹与 NFC 三项认证后才能进入。仅当 `AppConfig.current.isTestMode == true` 时，账号登录允许建立测试会话并跳过三项校验；人脸登录不受此旁路影响，正式部署前必须关闭。
2. **身份资料状态不可绕过**：正常、刚同步到本机和异常复核场景都必须完成人脸、指纹与 NFC 三项认证；缺失或异常的生物资料只进入人脸、指纹录入流程，失败留页重试，全部录入成功后返回首页重新登录。 当 `AppConfig.current.isTestMode == true` 时，人脸卡片必须明确使用模拟认证，不启动摄像头、不等待后端，且不得显示为真实后端校验成功；正式模式不得使用该旁路。
3. **任务仓库是最终授权边界**：存证、取证、借证、还证和盘点统一属于 `task_center`。路由门禁不能替代 Repository 对身份因子、操作员机构、任务机构、箱格机构、专用业务码、步骤顺序和完成条件的逐次校验；五类任务必须使用对应的完整固定步骤序列，平台数据不得删减、增加、重排或构造“待办后又完成”；取件码、飞检码等不得通过通用步骤完成入口绕过。
4. **五类任务必须保持业务语义**：存证和还证是受控存入，取证和借证是受控取出，但借证必须保留独立的借出闭环语义；盘点是按箱格完成“取出核对再放回”，不能退化成逐证件重复开门。未接入真实平台前，不得宣称已经形成借出记录、OCR 结论或平台业务闭环。
5. **盘点按箱格闭环**：比例抽盘按箱格计算；指定证件必须逐个存在且扩展为其所在箱格并盘点箱内全部证件；计划中的全部预期证件必须归属于计划箱格，不能靠漏列箱格跳过证件。同一箱格只开门一次，未抽中为灰、待盘为蓝、开门盘点中为橙、部分匹配为浅绿、全部正常且放回为绿、存在缺失/溢余/未确认放回为红。只有匹配柜门的明确关门确认才能把仍未扫描的预期证件判为缺失，超时或关闭弹窗不得判缺失。
6. **机构与箱格强隔离**：同一柜体内物理箱格按监管机构独占绑定。开门前必须同时核对账号机构、任务机构、箱格机构、当前证件门号与请求门号，禁止跨机构复用或仅依赖 UI 过滤；灰色箱格不得泄露其他机构的证件数量或明细。
7. **全局单柜门互锁**：任何模拟或真实开门动作都必须先通过业务授权，再取得应用级 `CabinetDoorGuard` 资格。每次开门周期使用唯一操作 ID，只有同门且同操作 ID 才能幂等或释放；其他请求不得抢占。超时、页面销毁或错误门号不得释放互锁；开门结果响应异常时先读取最新任务对账，无法确认时保留互锁，待人工关门并安全回退步骤后再释放。
8. **安全退出**：自动退出仅允许在无任务、无加载、无导航、无进行中动作且全部柜门关闭时运行；有柜门处于开启资格时，不得返回任务中心、手动退出或自动退出。
9. **异常留痕按需执行**：常规 RFID 盘点默认不要求每份证件拍照；缺失、溢余等异常可按平台任务要求拍摄箱格全景并上传。真实拍照、OCR、RFID、柜控板、门磁和平台接口未接入时，必须明确当前只是模拟边界。
10. **不恢复已删除旧模块**：不得重新创建 `features/storage`、`features/pickup`、`features/dropoff` 或 `features/flight_inspection`。飞检码与盘点规则继续归属 `task_center`；`core/storage` 是仍在使用的本地持久化基础设施，不属于旧业务模块。

## AI 工作流

1. 在分析、回答或修改代码前，先阅读全局规则以及与任务相关的 `README.md`、`ARCHITECTURE.md` 或专题文档。
2. 修改前查看工作区状态和相关差异；现有未提交或未跟踪内容视为用户改动，不覆盖、回滚、删除或据为己有。
3. 优先做最小、可维护、符合现有结构的改动。
4. 修改前先理解当前模块已有模式，不为了单次任务引入新的目录体系、状态方案、平台通道或构建链路。
5. 涉及 Dart 源码时，格式化实际改动的 Dart 文件，并优先运行 `flutter analyze` 与相关测试；跨模块或高风险改动再运行完整 `flutter test`。
6. 涉及 Android、摄像头、编码、RTSP、JNI 或预编译库时，按变更范围补充 Android 构建检查，并明确仍需真机验证的部分。
7. 规则、README 或纯文档改动通常不运行 Flutter 构建，但必须人工检查路径、命令和项目事实是否准确。
8. 不修改与当前任务无关的文件，不顺手格式化、重排或清理无关代码。
9. 涉及登录、身份资料、任务步骤、机构隔离或柜门状态时，必须联查 `home`、`identity_verification`、`task_center`、应用路由、多语言和对应安全回归测试。
10. 修改业务流程、倒计时或安全边界后，同步更新 `README.md` 与 `.agents/` 中的事实说明；不得让文档继续描述已经删除或尚未接入的能力。

## 适用范围

- 除非用户明确覆盖，否则全局规则一律视为强制要求。
- 如果用户需求与规则冲突，先说明冲突并澄清后再继续。
- 更深目录如果新增自己的 `AGENTS.md`，其规则只补充或收紧对应子目录要求；冲突时以离目标文件最近的规则为准。
- 除非规则系统本身或项目架构发生变化，否则不要随意改写本入口文件。

## 代码注释规范

- 新增或实质修改的类、函数和方法必须提供符合对应语言习惯的中文文档注释；简单框架覆写、显然的访问器和匿名回调可以由所属类型的完整注释覆盖，不重复解释样板代码。
- 复杂业务流程、异步竞态、资源生命周期、线程切换、平台通道和 JNI/RKMPP 逻辑必须添加行内注释说明设计意图与失败边界。
- 注释说明“为什么”和约束条件，不逐行复述代码；代码标识符、协议名和第三方 API 保留英文原文。

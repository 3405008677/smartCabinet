
仅限 X1 笔记本打包指令

$env:JAVA_HOME="E:\software\AndroidStudio\jbr"; $env:Path="$env:JAVA_HOME\bin;$env:Path"; dart run tool/build_release.dart


打包命令
必须先进入 Flutter 项目根目录执行：
E:\PQJ\App\SmartCabinet


自动更新应用版本并打包
dart run tool/build_release.dart --dart-define=AFRR_HOST=10.0.0.1 --dart-define=AFRR_PORT=16666 --dart-define=AFRR_SHELF_CODE=123456789012345 --dart-define=DEVICE_IMEI=123456789012345 --dart-define=STUM_DP=10.0.0.1

只生成本机或设备测试安装包（自动递增版本，不要求正式包名和生产证书）
dart run tool/build_release.dart --local-build

把以上示例值替换为本次设备的真实生产配置；脚本会校验并传给 Flutter，正式打包不使用源码样例默认值。

每次成功的正式打包都会自动递增两部分，例如：
1.0.0+1 -> 1.0.1+2 -> 1.0.2+3

`--local-build` 同样递增版本，并输出 debug 签名的可安装 APK；它只适合开发和现场测试，不能上传为生产 OTA 包。

点号前是 Android versionName，也是右下角、升级页和 STUM T03.VE 使用的版本号。
加号后是 Android versionCode；每个可升级 APK 都必须严格递增。
只有 APK 构建、版本校验和正式签名验证全部成功后，pubspec.yaml 才会更新。失败候选不得上传或分发；任何 APK 一旦离开本机，其版本必须视为已消耗，修复时人工指定更高版本。
正式制品位于 build/releases/，文件名包含版本号，并附带 SHA-256 和 STUM 使用的 MD5 摘要。
build/ 会被 flutter clean 删除，验收后必须把 APK 和两个摘要复制到受控发布库，不能只保留本地 build 目录。
当前本地锁只保护这一个工作区，正式包必须固定由唯一发布机串行生成。
android/key.properties 使用简单 key=value 单行格式；相对 storeFile 以 android/app/ 为基准，不使用续行、冒号分隔或 Java Properties 转义。

只预览下一版本，不构建也不修改文件：
dart run tool/build_release.dart --dry-run
该命令只计算版本，不检查正式包名、测试模式、生产参数或签名。

人工切换 minor 或 major 版本：
dart run tool/build_release.dart --version-name 1.1.0 --version-code 20 --dart-define=AFRR_HOST=10.0.0.1 --dart-define=AFRR_PORT=16666 --dart-define=AFRR_SHELF_CODE=123456789012345 --dart-define=DEVICE_IMEI=123456789012345 --dart-define=STUM_DP=10.0.0.1

当前缺少正式签名配置时，只做使用当前版本且不递增版本的未签名编译检查：
dart run tool/build_release.dart --allow-unsigned-compile-check

未签名编译检查产物不能用于生产 OTA。正式发布还要求 AppConfig.current.isTestMode 为 false，并在 android/key.properties 固定 applicationId、稳定 release 证书及 certificateSha256 指纹。
不要手改已忽略的 android/local.properties，也不要直接使用 flutter build apk --release 或 gradlew assembleRelease 作为自动发版入口。
新 APK 必须实际安装并重启应用，热重载不会改变 Android 包版本。


日常打包不要先执行 flutter clean。
flutter clean 会删除 Flutter、Gradle、Kotlin 和 RKMPP native 增量缓存，导致下一次打包变成全量构建，明显变慢。

只有遇到缓存损坏、资源不生效、native so 替换不生效时才执行：
flutter clean

日常打包不要执行 flutter pub upgrade。
pub upgrade 会改变依赖版本，可能触发更多重新编译，也会增加不可控风险。

只有计划升级依赖时才执行：
flutter pub upgrade

恢复依赖
flutter pub get


打调试包（debug，仅测试，不能上架）
直接打包，输出目录：build/app/outputs/flutter-apk/app-debug.apk
flutter build apk --debug

打 release 优化编译检查包（当前缺少正式签名配置，不能作为正式发布包，也不递增版本）
dart run tool/build_release.dart --allow-unsigned-compile-check
正式发布前必须配置独立 release keystore；当前项目缺少 android/key.properties 时不会回退到 debug key，而是生成未签名产物。


普通构建会打包 android/app/src/main/jniLibs/arm64-v8a/ 中的预编译 RKMPP native bridge，不需要额外参数。
普通构建不会重新编译 android/app/src/main/cpp/rkmpp_bridge.cpp；修改 C++ 后，必须另行重建并更新、验证预编译 so。

项目 Gradle 已固定 arm64-v8a；正式 release 无需也不得另行使用裸 flutter build 命令覆盖发布工具。


原生 Gradle 快速调试包（仅 Android 层，且必须先由 Flutter 刷新构建配置）
$env:JAVA_HOME="E:\software\AndroidStudio\jbr"; $env:Path="$env:JAVA_HOME\bin;$env:Path"; .\gradlew.bat :app:assembleDebug


服务器视频流重启
cd /data/PQJ/ZLMediaKit/release/linux/Release/

ss -ltnp | grep 554

kill -9 2293616

./MediaServer



当前 RTSP 链路
Camera2 -> ImageReader -> RKMPP H265 -> RtspTcpH265Publisher -> RTSP/TCP -> ZLMediaKit -> 播放器
当前已知参数：
- Android 720p 实际推流：1280x720@15fps / 2000kbps / GOP=1s
- Android 1080p 实际推流：1920x1080@15fps / 5000kbps / GOP=1s
- RTSP 发送队列：MAX_PENDING_FRAMES = 4
- Camera2 ImageReader 缓冲：2
- 传输：RTSP over TCP
- 推流方式：OPTIONS -> ANNOUNCE -> SETUP -> RECORD




以下耗时数据为旧版本现场采样，仅作参考；本次参数和队列调整后应以设备复测结果为准。
采集到编码入队这一段：
- 摄像头帧年龄平均：42-43ms
- 摄像头帧年龄最大：45-51ms
- native 编码平均：11-12ms
- native 编码最大：15-19ms
- RTSP 入队平均：0ms
- RTSP 入队最大：0-4ms
- 采集帧到进入 RTSP 队列平均：53-56ms
- 采集帧到进入 RTSP 队列最大：60-67ms
  RTSP 发送这一段：
- RTSP 队列等待平均：0ms
- RTSP 队列等待最大：1-2ms
- socket 实际发送平均：1-3ms
- socket 实际发送最大：4-24ms
- 当前队列长度：0
- 丢帧数：0

APK 端链路目前很轻：
- 采集到 RTSP 入队：大约 55ms
- RTSP 队列等待：基本 0ms
- RTSP socket 发送：平均 1-3ms
- APK 端没有队列积压、没有丢帧


- 摄像头底层/Camera2 固定缓冲
- ZLMediaKit 服务端缓冲
- 播放器缓存
- RTSP/TCP 传输和播放器解码缓冲
 

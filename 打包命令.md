
仅限 X1 笔记本打包指令

$env:JAVA_HOME="E:\software\AndroidStudio\jbr"; $env:Path="$env:JAVA_HOME\bin;$env:Path"; .\gradlew.bat :app:assembleRelease


打包命令
必须先进入这个目录执行：
E:\PQJ\App\SmartCabinet\android


日常打包不要先执行 flutter clean。
flutter clean 会删除 Flutter、Gradle、Kotlin 和 native 增量缓存，接入 GStreamer 后会导致下一次打包变成全量构建，明显变慢。

只有遇到缓存损坏、资源不生效、native so 替换不生效时才执行：
flutter clean

日常打包不要执行 flutter pub upgrade。
pub upgrade 会改变依赖版本，可能触发更多重新编译，也会增加不可控风险。

只有计划升级依赖时才执行：
flutter pub upgrade

恢复依赖
flutter pub get


打调试包（debug，仅测试，不能上架）
直接打包，输出目录：build/app/outputs/flutter-apk/app-debug.apk
flutter build apk --debug


快速打调试包（跳过 GStreamer native so，仅用于 UI 和普通业务验证）
flutter build apk --debug -PincludeGStreamer=false


打发布未签名包（release，无签名，需手动签名）
flutter build apk --release


正式打包默认包含 GStreamer，不需要额外参数。
如果只是临时验证非推流功能，也可以跳过 GStreamer：
flutter build apk --release -PincludeGStreamer=false

只适合 64 位 ARM Android 设备
flutter build apk --release --target-platform android-arm64
1


原生 Gradle 快速调试包（仅 Android 层）
$env:JAVA_HOME="E:\software\AndroidStudio\jbr"; $env:Path="$env:JAVA_HOME\bin;$env:Path"; .\gradlew.bat :app:assembleDebug -PincludeGStreamer=false


服务器视频流重启
cd /data/PQJ/ZLMediaKit/release/linux/Release/

kill -9 2293616

./MediaServer